apiVersion: v1
kind: Service
metadata:
  name: {{ include "bikeshop-api.fullname" . }}
  labels:
    {{- include "bikeshop-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "bikeshop-api.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
